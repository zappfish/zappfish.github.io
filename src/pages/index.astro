---
import Main from '../layouts/Main.astro'
import Hero from '../layouts/Hero.astro'
import HeroCTAItem from '../components/ui/HeroCTAItem.astro'
import FeatureCard from '../components/ui/FeatureCard.astro'
import { featureItems } from '../config/features'
---

<Main title="ZAPP">

    <Hero title="Zebrafish Toxicology Phenotype Atlas Project" image="/images/home-hero.jpg" slot="hero">
        <ul class="hero-ctas">
            {featureItems.map(item => (
                <HeroCTAItem title={item.title} href={item.href} icon={item.icon} />
            ))}
        </ul>
    </Hero>

    <section class="about">
        <h2>About ZAPP</h2>
        <p>Zebrafish as an animal model have greatly advanced toxicology and environmental studies, however, the lack of standards to report toxicological exposure and phenotypic outcomes impedes comparative studies.</p>
        <p>The Zebrafish Toxicology Phenotype Atlas Project, known as ZAPP or Zebrafish Atlas Phenotype Project, is creating a new zebrafish toxico-phenotype atlas filled with images annotated with terms from the <a href="https://obophenotype.github.io/zebrafish-phenotype-ontology/" target="_blank" rel="noopener">zebrafish phenotype ontology</a> standard and represents exposures using a new <a href="https://zappfish.org/zebrafish-toxicology-atlas-schema/" target="_blank" rel="noopener">toxico-pheno data model</a>.</p>
    </section>

    <section class="features">
        <h2>Get Started</h2>
        <div class="feature-cards">
            {featureItems.map(item => (
                <FeatureCard
                    title={item.title}
                    cardTitle={item.cardTitle}
                    description={item.description}
                    href={item.href}
                    btnLabel={item.btnLabel}
                    icon={item.icon}
                />
            ))}
        </div>
    </section>

    <section class="blog-preview">
        <h2>Recent Blog Posts</h2>
        <div class="blog-coming-soon">
            <p>Blog posts coming soon. Stay tuned for updates from the ZAPP community.</p>
        </div>
    </section>


</Main>

<style>
    /* ── About section ── */
    .about {
        background: var(--color-bg-white);
        border-radius: 10px;
        padding: var(--space-8) var(--space-12);
        box-shadow: 0 8px 16px -8px rgba(0, 0, 0, 0.15);
    }

    .about h2 { text-align: center; margin-bottom: var(--space-6); }

    .about p:last-child { margin-bottom: 0; }

    /* ── Hero CTA buttons ── */
    .hero-ctas {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        gap: var(--space-4);
        justify-content: center;
        flex-wrap: wrap;
    }

    /* ── Feature cards ── */
    .feature-cards {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: var(--space-6);
    }

    /* ── Blog preview ── */
    .blog-preview h2 { text-align: center; }

    .blog-coming-soon {
        text-align: center;
        padding: var(--space-12) var(--space-8);
        background: var(--color-bg-white);
        border: 2px dashed var(--color-border);
        border-radius: 10px;
        color: var(--color-text-muted);
    }

    .blog-coming-soon p { margin-bottom: 0; }


    @media (max-width: 900px) {
        .feature-cards { grid-template-columns: repeat(2, 1fr); }
    }

    @media (max-width: 600px) {
        .feature-cards { grid-template-columns: 1fr; }
        .hero-ctas { gap: var(--space-3); }
    }
</style>
