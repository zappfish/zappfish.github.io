---
interface Props {
    title?: string
    subtitle?: string
    iconColor?: 'green' | 'teal' | 'dark'
}
const { title, subtitle, iconColor = 'teal' } = Astro.props
const hasIcon = Astro.slots.has('icon')
const hasHeader = !!title || hasIcon
---
<div class="section-card">
    {hasHeader && (
        <div class="section-card-header">
            {hasIcon && (
                <div class={`section-icon section-icon--${iconColor}`}>
                    <slot name="icon" />
                </div>
            )}
            {title && (
                <div>
                    <h2>{title}</h2>
                    {subtitle && <p class="section-subtitle">{subtitle}</p>}
                </div>
            )}
        </div>
    )}
    <div class="section-card-body">
        <slot />
    </div>
</div>

<style>
    .section-card {
        background: var(--color-bg-surface);
        border-radius: var(--radius-lg);
        box-shadow: var(--shadow-card);
        overflow: hidden;
    }

    .section-card-header {
        display: flex;
        align-items: center;
        gap: var(--space-4);
        padding: var(--space-3) var(--space-6);
        background: var(--color-bg-tint);
        border-bottom: 1px solid var(--color-border);
    }

    .section-card-header h2 { margin-bottom: 0; }
    .section-subtitle { font-size: var(--font-size-ui); color: var(--color-text-muted); margin: var(--space-1) 0 0; }

    .section-icon {
        flex-shrink: 0;
        width: 28px;
        height: 28px;
        border-radius: 6px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--color-text-white);
    }
    .section-icon :global(svg) { width: 14px; height: 14px; color: var(--color-text-white); }
    .section-icon--green { background: var(--color-accent); }
    .section-icon--teal  { background: var(--color-primary-dark); }
    .section-icon--dark  { background: var(--color-primary); }
</style>
